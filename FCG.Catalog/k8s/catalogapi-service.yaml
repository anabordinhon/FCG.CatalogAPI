apiVersion: v1
kind: Service
metadata:
  name: catalogapi-service
  namespace: fcg
spec:
  type: LoadBalancer
  selector:
    app: catalogapi
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080